# ── Lambda dependencies ──────────────────────────────────────────────────────
boto3>=1.34.0
PyYAML>=6.0
Jinja2>=3.1.0

# ── Airflow runtime (modules loaded dynamically from S3 into MWAA) ───────────
apache-airflow>=2.9.0
apache-airflow-providers-amazon>=8.0.0
apache-airflow-providers-postgres>=5.0.0
apache-airflow-providers-ssh>=3.0.0

# ── Data / DB dependencies used inside job_runner & utils ────────────────────
psycopg2-binary>=2.9.0
redshift-connector>=2.1.0
pandas>=2.0.0
pendulum>=3.0.0
pytz>=2024.1
